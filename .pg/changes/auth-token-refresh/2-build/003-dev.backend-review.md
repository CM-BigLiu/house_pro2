# Review Report: dev.backend

> 变更：auth-token-refresh
> 阶段：review（静态代码审查，dev → review → verify 中间位置）
> effective profile：`vue3`（inherit `default`），language=typescript 自动派发

## Score

| Metric | Value |
|--------|-------|
| review_score | 100 |
| pass_threshold | 85 |
| disposition | completed |
| p0_failures | [] |

评分公式：`sum(weight for enabled & pass) / sum(weight for enabled) * 100`
= (30+25+20+15+10+15) / (30+25+20+15+10+15) * 100 = 100

## Profile

- Profile 名: vue3（inherit default，Union 合并）
- 检查项: 6 项 enabled
- 总权重: 115

## 检查项结果

| R-N | 检查项 | 权重 | 判定 | 证据 |
|------|--------|------|------|------|
| R-1 | design_alignment | 30 | PASS | refresh_token entity 7 字段齐全；login 双令牌 + refresh/logout/revokeUserTokens 均实现；/auth/refresh + /auth/logout 端点存在；JwtAuthGuard token_type=access 校验 |
| R-2 | scope_creep | 25 | PASS | 7 个变更文件全部在 code/server 内，均为 design.md「后端改动组件」明确列出的文件，无跨模块、无新 API/新表 |
| R-3 | pattern_consistency | 20 | PASS | NestJS 后端：@Injectable/@Controller/@Entity 装饰器齐全，构造器注入（非字段 @Autowired），Logger 日志一致 |
| R-4 | file_location | 15 | PASS | 全部文件位于 module root `code/server` 之下，无越界 |
| R-5 | test_contract | 10 | PASS | auth.service.spec.ts 11 用例 + jwt-auth.guard.spec.ts 4 用例，强断言，覆盖 V-backend-1~4，happy + error path 齐全 |
| R-6 | component_props | 15 | PASS | 本 track 为 backend（server 模块，NestJS TS），diff 中无任何 .vue 组件，规则无适用对象，无违规 |

## 逐项详细

### R-1 — design_alignment（PASS）

| design.md 承诺 | 代码位置 | 状态 |
|----------------|----------|------|
| `RefreshToken` entity（id/userId/tokenHash/status/expiresAt/createdAt/revokedAt） | `code/server/src/modules/auth/entities/refresh-token.entity.ts` | ✅ 7 字段全部实现，tokenHash unique index + userId index，timestamptz 类型一致 |
| `AuthService.login` 双令牌 + refresh 落库 sha256 hash | `auth.service.ts:64-103` | ✅ access(token_type=access,2h) + refresh(token_type=refresh,7d)；落库 `sha256(refreshToken)`，status=active，expiresAt=7d 绝对过期 |
| 新增 `refresh` / `logout` / `revokeUserTokens` | `auth.service.ts:105-191` | ✅ 三方法齐全，校验 token_type=refresh + DB 账态（active 且未过期） |
| `POST /auth/refresh`、`POST /auth/logout` | `auth.controller.ts:40-51` | ✅ `@Public()` + 独立校验，调用对应 service 方法 |
| `AuthModule` 注册 `TypeOrmModule.forFeature([RefreshToken])` + JWT 双 secret | `auth.module.ts:32` | ✅ forFeature([Employee, Role, Permission, RefreshToken])；refresh 用独立 secret + expiresIn |
| `JwtAuthGuard` 校验 token_type=access，拒绝 refresh 误用 | `jwt-auth.guard.ts:34-37` | ✅ `payload.token_type && !== 'access'` → 401，旧 token 无 token_type 视为 access（向后兼容，符合 design 明确要求） |

说明：design 提及「复用已有 sha256 能力（crypto.util.ts）」，但 `crypto.util.ts` 实为 AES-GCM 字段加解密，**无 sha256**，dev agent 在 auth.service.ts 内置 `createHash('sha256')` helper 是正确且必要的实现，不构成 FAIL。

### R-2 — scope_creep（PASS）

`git diff main...feat/pg/auth-token-refresh --name-only -- code/**` 只有 7 个文件，全部在 `code/server`，且逐一对齐 design「后端改动组件」表：

- refresh-token.entity.ts → 「新增」
- auth.service.ts → 「login 双令牌 + refresh/logout/revokeUserTokens」
- auth.controller.ts → 「新增 refresh/logout 端点」
- auth.module.ts → 「forFeature + JWT 双 secret」
- jwt-auth.guard.ts → 「token_type 校验」
- auth.service.spec.ts / jwt-auth.guard.spec.ts → test 阶段产物（test_contract 范畴）

无跨模块修改（frontend 未动），无 design 未提及的新 API/Entity/表。

### R-3 — pattern_consistency（PASS）

新增 entity 与既有 `Employee`/`Role`/`Permission` 同为 TypeORM `@Entity` + `@PrimaryGeneratedColumn` + 装饰器风格；AuthService 使用 `@Injectable()` + 构造器依赖注入（`@InjectRepository` / `JwtService` / `ConfigService`），非字段 `@Autowired`；新增 `@Controller('auth')` 端点沿用 `@Public()` + `@Post()` + DTO class-validator 校验（`LoginDto` 同款）。Logger 通过 `new Logger(AuthService.name)` 使用，符合 NestJS 惯例。Vue 特定规则（composable/useProTable/Pinia）不适用于后端 track。

### R-4 — file_location（PASS）

7 个文件全部位于 module_details server 的 `root: code/server` 之下，无任何文件落在 root 之外。

### R-5 — test_contract（PASS）

- **V-backend-1（落库 + 绝对过期）**：`auth.service.spec.ts:120` 断言 `expiresAt.getTime() === FIXED_NOW + 7d`（强断言）；`auth.service.spec.ts:168` 过期 token 刷新 401
- **V-backend-2（hash 非明文）**：`auth.service.spec.ts:133` 断言 `tokenHash` 匹配 `/^[0-9a-f]{64}$/` 且 `=== sha256(refreshToken)` 且 `!== refreshToken`
- **V-backend-3（停用联动吊销）**：`auth.service.spec.ts:181` revokeUserTokens 后 refresh 401
- **V-backend-4（token_type 区分）**：`auth.service.spec.ts:201` access token 用于 refresh 401；`jwt-auth.guard.spec.ts:34` refresh token 调业务接口 401

断言强度高（toBe/toEqual/rejects.toBeInstanceOf/toHaveBeenCalledWith），覆盖 happy + error path，命名反映业务场景（如「V-backend-1: refresh token 应落库为 active 记录…」）。

### R-6 — component_props（PASS）

component_props 是 Vue 3 组件 Props 检查（`defineProps`/`v-model`/`emit` 类型等）。本 track `dev.backend` 仅含 server 模块（NestJS TypeScript），diff 中没有任何 `.vue` 文件，检查项无适用对象。按「无违规即通过」判定为 PASS。

### implementation_completeness（N/A，非本 profile 检查项）

本 track effective profile 无 `implementation_completeness` 检查项，P0 硬约束清单为「（无）」。仍按协议额外核查：`code/server/src/**` 全文 grep `TODO|FIXME|stub|not implemented|placeholder|待实现` 无命中（仅命中 crypto.util.ts 既存的 `throw new Error('FIELD_ENCRYPTION_KEY...')` 生产密钥校验，非 TODO/stub）。无 task-V 缺失证据。

## 通过项（简要）

- R-1 design_alignment: PASS（6/6 后端承诺已实现）
- R-2 scope_creep: PASS（无跨模块，无越界功能）
- R-3 pattern_consistency: PASS（装饰器/DI/日志与既有一致）
- R-4 file_location: PASS（全部在 code/server）
- R-5 test_contract: PASS（V-backend-1~4 强断言全覆盖）
- R-6 component_props: PASS（后端 track 无 Vue 组件）

## 结论

- review_score 100 ≥ pass_threshold 85 → **completed**，进入 verify 阶段。
- p0_failures = []，无强制 escalate 项。
