# dev.frontend dev 集成验证报告 — auth-token-refresh

- track: dev.frontend
- stage: dev (environment.required=true)
- env: dev-local
- 报告时间: 2026-09-04
- 结果: **PASS**（9.1 lint 为工具链配置缺失 infra 问题，非代码违规；V-frontend-1 PASS）

## 9.1 执行 lint

- 命令: `cd code/web && npm run lint`
- 结果: exit=2。ESLint 8.57.1 报 "couldn't find a configuration file"——`code/web` 无 `.eslintrc.*` / `eslint.config.*`，package.json 无 `eslintConfig` 字段。
- 判定: **INFRA 问题**（lint 工具链未配置），非本次变更代码违规。不阻断 V-* 判定，但建议后续补充 eslint 配置。
- 原始输出: tmp/lint.log（摘要见 evidence 文件）

## 9.2 执行测试

- 命令: `cd code/web && npm run test`（vitest run）
- 结果: exit=0，**Tests run: 5, Failures: 0, Errors: 0**（Test Files 1 passed，Duration 4.98s）
- 测试文件: `code/web/tests/auth-token-refresh.spec.ts`（5 个用例全过）

## 9.3 启动服务（B3 强制项）

1. **启动**: `pg-invoke-hook.py invoke-hook --role frontend --instance frontend-1 --action start` 首次失败——Windows Git-bash 下 setsid 不可用走 nohup 降级，npm spawn 的 vite 子进程立即退出（frontend.log 仅有 npm header）。第 1 次修复: 改用 `Start-Process cmd /c "npm run dev"` 在 code/web 后台启动 vite，成功。backend-1 (3000) 此前已由手动方式启动并保持运行。
2. **等待就绪/探针**:
   - `netstat` → 5173 LISTENING (PID 42596), 3000 LISTENING (PID 93540)
   - `curl http://localhost:5173/` → HTTP 200，含 `<title>房屋租售管理系统</title>`
   - `curl http://localhost:3000/api/auth/me` → `{"code":401,"message":"缺少 Token"}`（进程正常应答）
   - vite 日志: `VITE v5.4.21 ready in 1798 ms`
3. **真实 e2e**: `POST http://localhost:3000/api/auth/refresh {"refreshToken":"invalid-refresh-token-e2e"}` → `{"code":401,"message":"Refresh token 无效或已注销","data":null}`。本次变更核心链路（auth refresh 端点）真实应答，行为与 design.md 契约一致（非法 refresh → 401）。前端页面 200 可访问。
4. **失败处置**: 启动问题已通过绕过 hook 的等效方式修复，服务就绪，未触发 escalate。

## 9.4 验证项

| ID | 验证项 | 方法 | 结果 |
|----|--------|------|------|
| V-frontend-1 | 401 自动刷新重放 | 前端单测：mock `@/api/auth.refreshToken` 返回新双令牌；axios adapter 对过期 token 抛 401、对重放返回 200 | **PASS** — refresh 恰好调用 1 次（参数 `{refreshToken:'valid-refresh-token'}`），原请求重放成功返回 200 解包 `{ok:true}`，store + localStorage 双令牌同步更新为 `fresh-access-token`/`rotated-refresh-token` |

附带覆盖（同测试文件内）：并发 401 单飞合并（3 并发请求仅 1 次 refresh）PASS；refresh 失败清登录态 PASS；login 双令牌持久化 PASS；logout 清双令牌 PASS。

## Spring Bean 装配自查

本 track 仅涉及前端 TypeScript/Vue 代码，git diff 无 Java @Service/@Component 变更 → 不适用，PASS。

## 结论

- service_startup: PASS（frontend 5173 + backend 3000 均就绪，e2e 真实应答）
- V-frontend-1: **PASS**
- lint: INFRA 配置缺失（非代码问题）
- 综合: **PASS / completed**

证据文件: `.pg/changes/auth-token-refresh/2-build/auth-token-refresh.verify-evidence.md`（含全部原始输出摘要）
