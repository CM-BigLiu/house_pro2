# Gate Assessment: dev.frontend

**Track**: dev.frontend
**Change**: auth-token-refresh
**Date**: 2026-09-04
**Cycle**: 1
**Verify report**: 2-build/009-dev.frontend-verify.md
**Review report**: 2-build/008-dev.frontend-review.md

## 整体判定

**PASS**

gate_score = 96.5 / 100
P0 failures: []

## 维度评分明细

| 维度 | 权重 | 得分 | 依据 |
|------|------|------|------|
| V-* 验证项通过率 | 50% | 100 | V-frontend-1 PASS（vitest 5/5 全过，含 refresh 重放正反例） |
| design.md 一致性 | 20% | 95 | 三个前端组件（api/auth.ts、stores/user.ts、utils/request.ts）完全落地双令牌契约；design.md 组件表写 `stores/auth.ts` 实际扩展既有 `stores/user.ts`，属命名沿用，扣 5 分但不阻断 |
| scope creep 检查 | 15% | 100 | 变更文件全部在 `code/web/` 模块根内；无未声明文件被改动；新增 package/vitest/test 均属测试基建 |
| 测试质量 | 10% | 95 | 5 个用例覆盖 happy path + refresh 失败 error path + 并发单飞合并 + 登录/登出持久化；断言均为强断言（toBe/toEqual/toHaveBeenCalledWith）；命名反映业务场景 |
| 实现完整性 (v3.x) | 10% | 100 | diff 中无 TODO/FIXME/stub/not implemented；唯一命中 `XXX` 为 package-lock.json sha512 hash（非真实标记） |
| runtime_boot (B3) | — | PASS | verify 报告 9.3 完整呈现启动/等待就绪/健康探针/真实 e2e 四步证据 |

gate_score = 0.5×100 + 0.2×95 + 0.15×100 + 0.1×95 + 0.1×100 = 96.5

## 检查项一览

| # | 检查项 | 判定 | 证据来源 |
|---|--------|------|---------|
| 1 | 本地 V-* 全部通过或已豁免 | ✅ | verify.md §9.4：V-frontend-1 PASS，5/5 测试全过 |
| 2 | 交付物与 design.md 一致 | ✅ | grep `code/web/src/{api/auth.ts,stores/user.ts,utils/request.ts}`：双令牌、refresh/logout API、单飞拦截器全部落地 |
| 3 | 无 scope creep | ✅ | `git diff main..feat/pg/auth-token-refresh --name-only -- code/web` 共 7 个文件全部在 `code/web/` 内；package.json 仅新增 vitest/jsdom/test script |
| 4 | 文件位置合规 | ✅ | 所有变更均在 module root `code/web` 下 |
| 5 | Evidence 充分 | ✅ | evidence 文件含 B3 服务启动+探针原始输出、lint 原始输出、test 日志摘要、V-frontend-1 断言细节、refresh e2e curl 响应 |
| 6 | 旧测试无回归 | ✅ | verify.md §9.2：Tests run 5, Failures 0, Errors 0；本 track 无既有测试需要回归（纯新增） |
| 7 | 证据可追溯（抽查） | ✅ | 抽查 V-frontend-1：测试文件 `code/web/tests/auth-token-refresh.spec.ts` 存在且用例名 `401 → 调 refresh → 重放原请求成功 (V-frontend-1)` 匹配；e2e 响应 `{"code":401,"message":"Refresh token 无效或已注销"}` 与 `AuthService.refresh` 401 分支吻合 |
| 8 | 测试质量 | ✅ | 见维度评分明细行 |
| 9 | 实现完整性硬标记扫描 | ✅ | 见下方专项核查段 |

## 实现完整性专项核查（v3.x 新增）

### 硬标记 grep 输出

命令：
```
git diff main..feat/pg/auth-token-refresh -- code/web | Select-String -Pattern '(TODO|FIXME|XXX|stub|not implemented|@todo|UnsupportedOperationException)'
```

命中 1 处：

```
785: +      "integrity": "sha512-YZo3K82SD7Riyi0E1EQPojLz7kpepnSQI9IyPbHHg1XXXevb5dJI7tpyN2ADxGcQbHG7vcyRHk0cbwqcQriUtg==",
```

**判定**：该命中为 `package-lock.json` 中 npm 依赖的 sha512 integrity hash，其中子串 `XXX` 纯属巧合，**并非真实的 TODO/FIXME 注释**。不计入命中。

**真实命中数 = 0** → 维度分 = 100。

### 豁免列表

无（未使用 `[skip-review]` 豁免）。

### tasks-V 映射核查

| Task | 预期关键 API/符号 | diff 中存在？ |
|------|-------------------|--------------|
| 6.1 前端单测 | `401 → 单飞 refresh → 重放原请求成功`、`store.login` 持久化双令牌 | ✅ `tests/auth-token-refresh.spec.ts` |
| 7.1 api/auth.ts | `login` / `refreshToken` / `logout` + `RefreshResult` | ✅ `src/api/auth.ts:39-45` |
| 7.2 stores/user.ts | `accessToken` / `refreshToken` / `setTokens` / `house_access_token` / `house_refresh_token` | ✅ `src/stores/user.ts:15-41` |
| 7.3 utils/request.ts | 401 拦截 / `doRefresh` 单飞 / `__isRetryAfterRefresh` / 失败跳登录 | ✅ `src/utils/request.ts:17-95` |
| V-frontend-1 | 401 自动刷新重放 | ✅ 测试 + 实现均已覆盖 |

**结论**：tasks 声明的所有关键符号在 diff 中均有对应实现，无"代码存在但未实质实现"的情况。

## runtime_boot（B3 硬约束）证据核验

verify 报告 §9.3 四步证据完整：

1. **启动**：`pg-invoke-hook.py --action start` 首次失败 → 改用 `Start-Process cmd /c "npm run dev"` 启动 vite（PID 42596）
2. **等待就绪**：`netstat` 显示 5173 LISTENING + 3000 LISTENING；vite 日志 `VITE v5.4.21 ready in 1798 ms`
3. **健康探针**：
   - `curl http://localhost:5173/` → HTTP 200 含 `<title>房屋租售管理系统</title>`
   - `curl http://localhost:3000/api/auth/me` → `{"code":401,"message":"缺少 Token"}`
4. **真实 e2e**：`POST /api/auth/refresh {"refreshToken":"invalid-refresh-token-e2e"}` → `{"code":401,"message":"Refresh token 无效或已注销"}`，证明后端 refresh 端点真实应答、行为符合 design.md 契约（非法 refresh → 401）

四步齐全 → B3 P0 通过。

## 证据抽查记录（步骤 1.5）

抽查 2 个 V-* 项（本 track 仅 1 个 V-*，加上 lint/test 原始证据共 2 项）：

| 抽查项 | 追溯方式 | 结果 |
|--------|---------|------|
| V-frontend-1 | glob `code/web/tests/auth-token-refresh.spec.ts` 存在；grep 用例名 `401 → 调 refresh → 重放原请求成功` 匹配 spec 文件第 118 行 | ✅ 可追溯 |
| 测试日志 `Tests 5 passed (5)` | verify.md §9.2 引用 `tmp/test.log`；evidence 文件 §9.2 含 vitest 输出摘要 | ✅ 可追溯 |

`2/2 项证据可追溯`

## 回归分析（步骤 1.6）

- 本 track 为纯新增功能，无历史测试需要回归。
- vitest 5 passed / 0 failed / 0 errors。
- **预存失败 0 个 / 新引入 0 个**。

## 不通过项详细说明

无 FAIL 项。

## 关键观察（不阻断）

1. **lint INFRA 问题**：`code/web` 缺 eslint 配置文件（`exit=2`），verify agent 判定为工具链配置缺失（非代码违规），不阻断 V-*。建议后续在独立变更中补充 `.eslintrc.*`。
2. **design.md 组件表路径漂移**：design.md 前端组件表写 `code/web/src/stores/auth.ts`，实际扩展既有 `code/web/src/stores/user.ts`。属既有命名沿用（项目原本只有 user store），非新文件漂移，review agent 已记录并未判 FAIL。
3. **review R-3 pattern_consistency FAIL（非 P0）**：`request.ts` 拦截器直接对 Pinia state 赋值（`userStore.userInfo = null`），review 建议抽取 `clearAuthState()` action。review_score 82.6 ≥ 80 阈值，未阻断。

## 整体判定

**PASS** — gate_score 96.5 ≥ 80 且无 P0 失败，dev.frontend track 门控通过，可进入 int stage。
