PASS

# Verify Report: int.backend — auth-token-refresh

> 阶段：verify（int 集成验证）｜环境：dev-local（environment.required=true，不可 SKIP）
> 服务：backend-1 localhost:3000（`nest start --watch` 运行中，PID 124572，LISTENING 3000）
> 证据文件：`2-build/auth-token-refresh.verify-evidence.md`

## 结果总览

| 任务 | 结果 | 说明 |
|------|------|------|
| 14.1 lint | FAIL（既存豁免） | 项目缺少 ESLint 配置文件（`ESLint couldn't find a configuration file`），与 dev.backend verify 一致，非本次变更引入 |
| 14.2 测试 | PASS | `npm test`：Test Suites 3/3，Tests 21/21 passed，Failures 0，Errors 0 |
| 14.3 启动服务 | PASS | backend-1 port 3000 LISTENING（PID 124572），`/api/auth/refresh` 端点存活 |
| 14.4 V-backend-1 | PASS | login 后 `refresh_token` 表新增 active 记录；expiresAt−createdAt=7.00d（绝对过期）；status=active |
| 14.4 V-backend-2 | PASS | DB `tokenHash` = sha256(refreshToken)（64 hex，与明文 sha256 逐字节一致）；明文不落库（hash==plaintext? false） |
| 14.4 V-backend-3 | PASS | logout 后 DB 行 status→revoked + revokedAt 落时间；再用该 refreshToken 刷新 → 401「Refresh token 无效或已注销」 |
| 14.4 V-backend-4 | PASS | refresh token 调 /auth/me → 401；access token 作 refreshToken 调 /auth/refresh → 401；access token 调 /auth/me → 200 |

## 运行时启动 + 健康探针（B3 强制项）

1. **启动**：backend-1 (port 3000) 由 `npm run start:dev`（nest start --watch）保持运行，`netstat` 确认 3000 LISTENING（PID 124572）。
2. **就绪探针**：HTTP 探针 `/api/health` → 404（无根路由，正常，全局前缀 `/api`）；`POST /api/auth/login` 返回正常业务应答，证明服务可用。
3. **真实 e2e**：login → 得双令牌（access 2h / refresh 7d，token_type 正确）→ DB 落库核验（V-backend-1/2）→ 类型隔离三向核验（V-backend-4）→ logout 吊销（V-backend-3）→ 再刷新 401。全链路原始输出见 evidence 文件。
4. **失败处置**：无失败。

## NestJS DI 装配自查

AuthService 单构造函数，构造器注入 `@InjectRepository(Employee/Role/RefreshToken)` + `JwtService` + `ConfigService`，全部 provider 已在 AuthModule 注册；无多构造函数、无"测试替身+生产 stub"模式。服务已成功启动运行即装配正确。

## Issues

无代码 bug。lint 失败为项目既存 ESLint 配置缺失（非本次变更引入，dev/int 阶段均已记录），不构成 escalate。

## 结论

**PASS** —— V-backend-1~4 全部在真实运行服务 + 真实 PostgreSQL 上验证通过，集成测试 21/21 PASS。track `int.backend` verify 阶段判定 `completed`。
