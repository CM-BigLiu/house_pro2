## 任务：dev.backend

### 变更名称
auth-token-refresh

### Track 配置
- track.id: dev.backend
- track.modules: ['server']
- track.max_fix_retries: 5

### Module 配置
  - module: server
    - root: code/server
    - language: typescript
    - build: {'cmd': 'cd code/server && npm run build', 'timeout_seconds': 600}
    - lint: {'cmd': 'cd code/server && npm run lint', 'timeout_seconds': 300}
    - test.unit: {'cmd': 'cd code/server && npm test', 'timeout_seconds': 900}
    - test.integration: cd code/server && npm run test:e2e

### Stage 配置
- stage.name: dev
- stage.gate: all_pass
- stage.environment.required: True
- stage.environment.name: dev-local
- stage.environment.prepare.status: ok
- stage.environment.prepare.log_path: 
- stage.test_commands: cd code/server && npm test


---

### 门控审查要求（v2.1 加权评分）

审查上一轮 verify 报告，逐项核查 tasks.md 的 G-* 门控项，
**并对每个维度独立打分**（0-100），最后综合计算 gate-score。

### 🔴 v3.x 新增：实现完整性维度（10%，P0 硬约束）

**必须**对 `git diff feat/pg/auth-token-refresh` 单独执行实现完整性核查。
这是 v3.x 新增的硬约束，防止"代码存在但未实质实现"绕过 verify 阶段的 MockMvc 测试。

执行命令：

```bash
# 硬标记扫描
git diff feat/pg/auth-token-refresh | grep -nE '//\s*(TODO|FIXME|XXX|stub|not implemented)|@todo|UnsupportedOperationException' | grep -v '\[skip-review\]'

# tasks-V 映射核查（人工提取 tasks.md 关键 API 关键词，在 diff 中 grep）
```

判定：

- 命中 ≥ 1 处（除豁免）→ 维度分 = 0，整个 gate FAIL
- 豁免 ≥ 3 → 维度分 = 0，整个 gate FAIL
- 完全 PASS → 维度分 = 100

### 加权评分维度

| 维度 | 权重 | P0 失败条件（即使总分高也 FAIL） |
|------|------|-------------------------------|
| V-* 验证项通过率 | 50% | 任何 V-* FAIL |
| design.md 一致性 | 20% | 缺核心 API/DTO 改动 |
| scope creep 检查 | 15% | 修改了未在 proposal 声明的文件 |
| 测试质量 | 10% | 新代码无单测覆盖 |
| **实现完整性**（v3.x） | **10%** | **diff 中存在 TODO/stub 或 task-V 缺失 → 整体 FAIL** |
| **runtime_boot**（B3 修复） | — | **verify 报告缺"运行时启动+健康探针"4 步证据 → 整体 FAIL** |

> v3.x 起实现完整性维度占 10% 权重，原"测试质量"维度相应降为可选 0%（合并到实现完整性）。
> **runtime_boot**（B3 修复）：无独立权重，但作为 P0 硬约束——若 verify 报告
> 缺少"启动服务 / 等待就绪 / 真实 e2e / 失败处置"4 步中任一步的原始输出或
> 日志片段，则 gate 直接 FAIL。防止"代码存在但装配失败"（如多构造函数缺
> @Autowired）漏到集成环境。

### gate-score 计算

```
gate-score = Σ(维度分 × 权重)  (0-100)
PASS 判定: gate-score ≥ 80 AND 无 P0 失败
FAIL 判定: gate-score < 80 或 存在 P0 失败
```

### 判定

- **PASS**：gate-score ≥ 80 且无 P0 失败，返回 `gate pass`
- **FAIL**：gate-score < 80 或存在 P0 失败，返回 `gate fail`（需列出具体 G-N 项 + 维度分）
- **🔴 P0 触发条件**（v3.x）：实现完整性维度 FAIL → 无论 gate-score 多高，整体 FAIL

### 返回契约

除 Sub-agent 返回契约的 6 字段外，还必须包含：
- 在 summary 中附加: `"gate_score: <0-100>, p0_failures: [G-N, ...]"`
- 在 report_path 文件中提供每个维度的分数明细
- **必须**含实现完整性维度的 grep 输出与豁免列表

### 写盘要求

完成审查后，自行用 cat > 写盘到 2-build/005-dev.backend-gate.md。
格式：Markdown，包含：
- 每个 G-* 项的结论（PASS / FAIL）及依据
- 每个维度的分数（0-100）和权重
- 最终 gate-score 和 PASS/FAIL 判定
- **实现完整性核查段**：含 grep 输出、豁免列表、tasks-V 映射核查结论

---

### 执行要求

**任务清单**：
## 5. dev.backend:gate - dev 门控审查

<!-- on_conditions_eval:
     stage=dev (常驻, 无 on_conditions)
     track=backend (常驻, 无 on_conditions)
-->

- 无

**验证要求**：| ID | 验证项 | 前置/数据准备 | 方法 | 预期结果 |
|-----|--------|---------------|------|---------|
| V-backend-1 | refresh token 落库 + 绝对过期 | 有可登录员工（sys_employee 有记录） | 单元测试调 `login` → 断言 `refresh_token` 表新增 active 记录且 expiresAt = 签发时间 + 7d；过期 token 刷新返回 401 | 落库成功，过期时间正确，过期后刷新 401 |
| V-backend-2 | refresh token 存 hash 非明文 | 同上 | 单元测试调 `login`，断言 `refresh_token.tokenHash` 为 sha256（64 hex），且不等于返回给前端的明文 refreshToken | 落库为 hash，明文不落库 |
| V-backend-3 | 员工停用联动吊销 | 有员工记录 | 单元测试 `revokeUserTokens(userId)` 后，该用户历史 refresh token 刷新返回 401 | 停用/吊销后刷新失败 |
| V-backend-4 | token_type 区分 | 已签发 access + refresh token | 用 refresh token 调带 `JwtAuthGuard` 的业务接口 → 401；用 access token 调 `refresh` → 401 | 类型隔离正确 |

---

## Sub-agent 返回契约

**完成后调用 `pg-build-result` 生成返回值并落盘，不要手写 JSON。**

### 必填字段（本阶段：gate）

| 字段 | 本阶段要求 |
|------|-----------|
| `--status` | 必填（见下方 status 矩阵） |
| `--summary` | 必填，<= 500 字 |
| `--track` | 必填（如 `dev.backend`） |
| `--phase` | 必填（当前阶段） |
| `--report` | **必填**（报告文件绝对路径，文件必须已写盘） |
| `--evidence` | **至少 1 个**（证据文件绝对路径，可多次传） |
| `--tasks-updated` | 选填 |
| `--outputs` / `--issues` | 选填 |

### status 矩阵（本阶段允许）

| phase=`gate` | `pass` / `fail` |

### 调用命令

```bash
python3 .pg/skills/src/core/workflows/skills/pg-build/scripts/pg-build-result \
    --mode agent \
    --status <status> \
    --summary "<=500 字>" \
    --track <dev.xxx> \
    --phase gate \
    --output-path D:\04-profile\test\house_pro4\house_pro2\.pg\changes\auth-token-refresh\2-build\005-dev.backend-gate-result.json \
    --require-output \
    [--report <绝对路径>] \
    [--evidence <绝对路径>]... \
    [--outputs <p1>,<p2>,...] \
    [--issues "<issue1>,<issue2>,"] \
    [--tasks-updated <id>]...
```

- `D:\04-profile\test\house_pro4\house_pro2\.pg\changes\auth-token-refresh\2-build\005-dev.backend-gate-result.json` 来自本 dispatch 上下文注入，**直接照抄**
- **A3 修复（v3.x）**：result.json 缺失 = orchestrator 直接 fatal `result_json_missing_after_retry`，**不再自动重试**（v2.4 重试机制会让一次 sub-agent 卡死掩盖整个 track 失败）。必须严格按命令模板执行 `--output-path D:\04-profile\test\house_pro4\house_pro2\.pg\changes\auth-token-refresh\2-build\005-dev.backend-gate-result.json --require-output`，不要漏写。
- 脚本 exit 1 → 字段校验失败，stderr 输出错误信息，照提示修复后重跑
- **gate / final-gate**：summary 末尾追加 `"gate_score: <0-100>, p0_failures: [...]"`，否则 schema_violation