# Gate Assessment: int.backend

**Track**: int.backend
**Change**: auth-token-refresh
**Date**: 2026-09-08
**Cycle**: 1

## 整体判定

**FAIL** — gate_score: 76 < 80，且触发 P0 硬约束（scope creep：变更 diff 中包含 proposal/design 未声明的 house/system/seeds 模块改动）。

## 检查项

| # | 检查项 | 判定 | 证据来源 |
|---|--------|------|---------|
| 1 | 本地 V-* 全部通过或已豁免 | ✅ | 014-int.backend-verify.md：V-backend-1~4 全部 PASS（真实服务+真实 PostgreSQL e2e），无 SKIP；14.2 集成测试 21/21 PASS；14.1 lint FAIL 已标注为既存 ESLint 配置缺失（非本次变更引入） |
| 2 | 交付物与 design.md 一致 | ✅ | design「后端改动组件」6 项承诺全部落地：refresh-token.entity（38 行/7 字段+tokenHash unique + userId index）、AuthService.login 双令牌签发+落库 hash、新增 refresh/logout/revokeUserTokens、AuthController 新增 POST /auth/refresh + /auth/logout（@Public）、AuthModule forFeature([RefreshToken]) + JWT_ACCESS_EXPIRES_IN、JwtAuthGuard token_type==='access' 校验（guard.ts:34-37） |
| 3 | 无 scope creep | ❌ | 见 G-1：`git diff afd1483..HEAD --name-status -- code/` 命中 19 个非授权文件，共 676 行新增，全部位于 `code/server/src/modules/house/`（checkout/deposit controller+service+entity）、`code/server/src/modules/system/`（city/config/log controller+service+entity）、`code/server/src/database/seeds/`（biz/dict/seed.module/seed.service）；proposal.md「包含」清单未声明任何一项 |
| 4 | 文件位置合规 | ⚠️ | 所有变更文件均在 module root `code/server` 之下（位置合规）；但其中 19 个文件不属于本 track 已声明范围（属 #3 scope creep） |
| 5 | Evidence 充分 | ✅ | auth-token-refresh.verify-evidence.md 含 14.1 lint 原始输出、14.2 测试 21/21 摘要、14.3 B3 四步（启动+netstat LISTENING 124572+/api/health 404 进程应答+真实 e2e+无失败）、14.4 V-backend-1~4 真实 curl/DB 输出（sha256 hash 比对 match、logout 后 status=revoked+revokedAt 落时间、refresh-after-logout 401、token_type 三向隔离） |
| 6 | 旧测试无回归（含新引入分析） | ✅ | Test Suites 3/3、Tests 21/21、Failures 0；lint FAIL 为预存既存问题（002-dev.backend-dev-result.json + 004-dev.backend-verify 4.1 双重佐证），新引入 0 个 |
| 7 | 证据可追溯（抽查） | ✅ | 抽查 2/2：① V-backend-2 sha256 落库 → auth.service.ts:5 `const sha256 = (s: string) => createHash('sha256').update(s).digest('hex')` + login 段 `tokenHash: sha256(refreshToken)` + refresh 段 `where: { tokenHash: sha256(refreshToken) }`；② V-backend-4 token_type 隔离 → jwt-auth.guard.ts:34-37 `if (payload?.token_type && payload.token_type !== 'access') throw UnauthorizedException` |
| 8 | 测试质量 | ✅ | 集成测试 6 用例（11.1-V1~V4 + FULL + EDGE）命名直接映射 V-* 编号；强断言（toBe(201/401)、toMatch(/^[0-9a-f]{64}$/)、getTime() 区间断言）；happy+error+edge 路径成对；唯一 1 处弱断言 `expect(row).not.toBeNull()` 紧随其后有 tokenHash 强断言弥补 |
| 9 | 结构化输出 | ✅ | FAIL 项已按 `{track}:G-{N}` + 必填字段输出 |

## 加权评分维度

| 维度 | 权重 | 得分 | 依据 |
|------|------|------|------|
| V-* 验证项通过率 | 50% | 100 | V-backend-1/2/3/4 全部 PASS（真实服务+真实 PostgreSQL e2e，非仅 Mock/单测）；集成测试 21/21 PASS |
| design.md 一致性 | 20% | 100 | 后端 6 项组件承诺（RefreshToken entity / AuthService 三方法 / AuthController 两端点 / AuthModule forFeature+双 secret / JwtAuthGuard token_type 校验 / crypto sha256 内置 helper）全部匹配实际代码 |
| scope creep 检查 | 15% | 0 | **P0 触发**——19 个文件、676 行新增属于 proposal/design 未声明的功能（house/checkout、house/deposit、system/city、system/config、system/log、seeds 数据），详见 G-1 |
| 测试质量 | 10% | 90 | 强断言 + 正反路径覆盖；扣 10 分：1 处 `expect(row).not.toBeNull()` 弱断言（紧随 tokenHash 强断言弥补） |
| 实现完整性（v3.x） | 10% | 100 | 硬标记扫描 0 命中（见下方核查段）；tasks-V 映射全部在 diff 中定位 |
| runtime_boot（B3 硬约束） | P0 | 满足 | verify 报告 14.3 含 4 步完整证据：① 启动（backend-1 PID 124572 LISTENING 3000）② 就绪探针（/api/health 404 进程正常应答 + login 业务应答 401）③ 真实 e2e（login→双令牌→落库→类型隔离三向→logout→401）④ 失败处置（无失败） |

**gate-score = 100×0.50 + 100×0.20 + 0×0.15 + 90×0.10 + 100×0.10 = 76**
**P0 失败 = 1 项（scope creep：修改了未在 proposal 声明的文件）**

## 实现完整性核查段（v3.x P0）

### 硬标记扫描

执行命令（等价于 dispatch 模板）：
```
git diff afd1483..HEAD -- code/ | grep -nE '//\s*(TODO|FIXME|XXX|stub|not implemented)|@todo|UnsupportedOperationException'
```

**命中数：0**（无任何 TODO/FIXME/XXX/stub/not implemented/@todo/UnsupportedOperationException 标记）

补充校验：`grep -r "todo|fixme|stub|not implemented" code/server/src` 仅命中 `dashboard/dashboard.controller.ts` 等业务字段名 `todos`（复数变量），非硬标记。

### 豁免列表

无（0 命中，无需豁免）。

### tasks-V 映射核查（tasks.md 关键 API 关键词在 diff 中定位）

| tasks.md 任务 | 关键词 | diff 中位置 | 状态 |
|---------------|--------|-------------|------|
| 11.1 int 测试覆盖 V-backend-1~4 | `int.spec.ts` / `V-backend` | code/server/src/int/auth-token-refresh.int.spec.ts 全文新增（369 行，6 用例 11.1-V1~V4/FULL/EDGE） | ✅ |
| 12.1 无新增实现（dev 已完成） | — | int 阶段 dev 步骤无新增源码（仅 pipeline 元数据落盘） | ✅ |
| 2.1 RefreshToken entity | `refresh_token` / `RefreshToken` | entities/refresh-token.entity.ts（38 行 7 字段：id/userId/tokenHash/status/expiresAt/createdAt/revokedAt；tokenHash unique + userId index） | ✅ |
| 2.2 AuthModule 注册 + JWT 双 secret | `forFeature([...RefreshToken])` / `JWT_REFRESH_SECRET` / `JWT_ACCESS_EXPIRES_IN` | auth.module.ts:+23,+24-28,+32 | ✅ |
| 2.3 login 双令牌 + 落库 hash | `token_type: 'access'` / `token_type: 'refresh'` / `sha256(refreshToken)` | auth.service.ts login 段（expiresAt=now+7d 绝对过期，落库 save） | ✅ |
| 2.4 refresh 方法 | `async refresh(` / `payload?.token_type !== 'refresh'` / `record.status !== RefreshTokenStatus.Active` | auth.service.ts:105+ | ✅ |
| 2.6 新端点 | `@Post('refresh')` / `@Post('logout')` / `@Public()` | auth.controller.ts:+40-51 | ✅ |
| 2.7 Guard token_type 校验 | `payload?.token_type && payload.token_type !== 'access'` | jwt-auth.guard.ts:34-37 | ✅ |
| V-backend-1~4 | spec 用例 it() 标题直接含 V-backend-N 编号 | auth.service.spec.ts（11 用例）+ jwt-auth.guard.spec.ts（4 用例）+ auth-token-refresh.int.spec.ts（6 用例） | ✅ |

**结论**：tasks.md 全部 8 个 int+dev 任务关键词均在 diff 中定位，无"代码存在但未实质实现"。实现完整性维度 = 100，P0 未触发。

## 回归分析

- 预存失败 1 个：`npm run lint`（项目缺 ESLint 配置文件，既存问题）——002-dev.backend-dev-result.json + 004-dev.backend-verify 4.1 + 014-int.backend-verify 14.1 三处记录佐证非本次变更引入
- 新引入失败 0 个：单测+集成测试 21/21 PASS、服务真实启动成功（PID 124572 LISTENING 3000）
- 注：`AuthService.login` 返回结构 `{token, user}` → `{accessToken, refreshToken, user}` 为 design 明确声明的破坏性变更，前端同步改动属 int.frontend track 范围，不在本 track 判定内

## 不通过项详细说明

### int.backend:G-1 — scope creep：house/system/seeds 模块超出 proposal 声明范围

- **检查项**: #3（无 scope creep）
- **预期**: 变更 diff 仅包含 proposal.md「包含」清单声明的文件（auth/refresh-token 相关 + 前端登录态/拦截器）；「不包含」清单（OAuth/多设备UI/reuse detection/Redis 黑名单/sys_employee 密码重置）外的功能不应出现
- **实际**: `git diff afd1483..HEAD --name-status -- code/` 命中 19 个 proposal/design 未声明的文件，共 676 行新增：

  **house 模块（退租/押金，新增完整业务闭环）**：
  - `code/server/src/modules/house/controllers/checkout.controller.ts`（+70 行，CRUD + confirm + complete 端点）
  - `code/server/src/modules/house/controllers/deposit.controller.ts`（+38 行）
  - `code/server/src/modules/house/entities/checkout.entity.ts`（+49 行）
  - `code/server/src/modules/house/entities/deposit.entity.ts`（+49 行）
  - `code/server/src/modules/house/services/checkout.service.ts`（+72 行）
  - `code/server/src/modules/house/services/deposit.service.ts`（+56 行）
  - `code/server/src/modules/house/house.module.ts`（+14 行注册新实体/服务）

  **system 模块（城市/配置/日志）**：
  - `code/server/src/modules/system/controllers/city.controller.ts`（+14 行）
  - `code/server/src/modules/system/controllers/config.controller.ts`（+41 行）
  - `code/server/src/modules/system/controllers/log.controller.ts`（+14 行）
  - `code/server/src/modules/system/entities/config.entity.ts`（+34 行）
  - `code/server/src/modules/system/services/city.service.ts`（+17 行）
  - `code/server/src/modules/system/services/config.service.ts`（+36 行）
  - `code/server/src/modules/system/services/system-log.service.ts`（+101 行）
  - `code/server/src/modules/system/system.module.ts`（+15 行注册）

  **seeds 数据（与上述业务闭环配套）**：
  - `code/server/src/database/seeds/biz.seed.ts`（+37 行，新增 checkout/deposit/operationLog/config 造数）
  - `code/server/src/database/seeds/dict.seed.ts`（+10 行，新增 checkout_status/deposit_status 字典）
  - `code/server/src/database/seeds/seed.module.ts`（+6 行注册新实体）
  - `code/server/src/database/seeds/seed.service.ts`（+14 行注册新权限点 checkout:confirm / deposit:refund / deposit:deduct）

- **文件位置**: 见上列表
- **关联 task**: int.backend:dev 任务 12.1, int.backend:review 任务 13.1~13.4, dev.backend:dev 任务 2.1~2.7
- **修复建议**: 这些文件源自 `b94e7ad feat(退租管理): 补齐退租单创建/确认/完成闭环接口与页面改造` 等历史提交，未在 auth-token-refresh 提案中声明。修复方向：将 house/system/seeds 相关变更从 feat/pg/auth-token-refresh 分支剥离（rebase 或单独 cherry-pick 到独立变更），保留 auth/refresh-token 相关 + 前端登录态/拦截器改动；若确认这些业务闭环属于本变更一部分，需回到 pg-propose 阶段补充 proposal/design/tasks 声明范围并重新走 review/verify。

## 整体判定

**FAIL** — gate_score 76 < 80，且触发 P0 硬约束（scope creep 维度 0 分）。

V-* 验证项全部 PASS、design.md 一致性 100、实现完整性 100、runtime_boot 4 步证据齐备——本变更核心功能（auth-token-refresh）实现完整、验证充分。但 diff 中包含 proposal/design 未声明的 19 个 house/system/seeds 模块文件（676 行新增业务代码），构成 scope creep P0 失败，必须先解决范围合规问题后方可交付。
