## 任务：dev.backend

### 变更名称
auth-token-refresh

### Track 配置
- track.id: dev.backend
- track.modules: ['server']
- track.max_fix_retries: 5

### Module 配置
  - module: server
    - root: code/server
    - language: typescript
    - build: {'cmd': 'cd code/server && npm run build', 'timeout_seconds': 600}
    - lint: {'cmd': 'cd code/server && npm run lint', 'timeout_seconds': 300}
    - test.unit: {'cmd': 'cd code/server && npm test', 'timeout_seconds': 900}
    - test.integration: cd code/server && npm run test:e2e

### Stage 配置
- stage.name: dev
- stage.gate: all_pass
- stage.environment.required: True
- stage.environment.name: dev-local
- stage.environment.prepare.status: ok
- stage.environment.prepare.log_path: 
- stage.test_commands: cd code/server && npm test


---

- stage.environment.instances:
```yaml
backend:
- name: backend-1
  host: localhost
  port: 3000
frontend:
- name: frontend-1
  host: localhost
  port: 5173
```

- stage.environment.hooks:
```yaml
backend:
  start:
    script: .pg/hooks/role-backend-start.sh
    host: localhost
    timeout_seconds: 120
  stop:
    script: .pg/hooks/role-backend-stop.sh
    host: localhost
    timeout_seconds: 30
  logs:
    script: .pg/hooks/role-backend-logs.sh
    host: localhost
    timeout_seconds: 30
frontend:
  start:
    script: .pg/hooks/role-frontend-start.sh
    host: localhost
    timeout_seconds: 120
  stop:
    script: .pg/hooks/role-frontend-stop.sh
    host: localhost
    timeout_seconds: 30
  logs:
    script: .pg/hooks/role-frontend-logs.sh
    host: localhost
    timeout_seconds: 30
```

### 运行时环境操作指令

如需启动/停止服务或查看日志，使用：

```
python3 .pg/skills/src/runtime/bin/pg-invoke-hook.py invoke-hook \
  --session auth-token-refresh --env dev-local --role <ROLE> --instance <INSTANCE> \
  --action <ACTION> [--tail-lines <N>] --skill pg-build
```

| action | 用途 |
|--------|------|
| start  | 启动某 role 实例 |
| stop   | 停止某 role 实例 |
| logs   | 查看完整日志 |
| tail   | 查看末尾 N 行日志 |

**ROLE / INSTANCE 来源**：
- **ROLE**：从上方 `stage.environment.hooks` 的顶层 key 选取（如 `backend` / `agent` / `frontend`）
- **INSTANCE**：从 `stage.environment.instances.<ROLE>` 列表中选取 `name` 字段（如 `backend-1` / `agent-1` / `frontend-1`）
- **示例**：若要启动 backend 服务并查看日志，命令为：
  ```
  python3 .pg/skills/src/runtime/bin/pg-invoke-hook.py invoke-hook \
    --session auth-token-refresh --env dev-local --role backend --instance backend-1 \
    --action start --skill pg-build
  ```

---

### 实现要求

本阶段需要根据 design.md 实现功能代码。遵循以下原则：
- 只创建和修改生产代码
- 不删除已有测试
- 确保测试仍然通过
- 如果现有测试需要修改以适配新实现，那也是允许的

### 模块路径约束

禁止编辑以下路径的文件：
- 不在 ['code/server'] 路径下的文件

### 完善清单

- [ ] 所有 checkstyle / lint 检查 0 警告
- [ ] 所有单元测试 PASS
- [ ] 集成测试 PASS（如环境可用）

---

### 执行要求

**任务清单**：
## 2. dev.backend:dev - 实现开发

<!-- on_conditions_eval:
     stage=dev (常驻, 无 on_conditions)
     track=backend (常驻, 无 on_conditions)
-->

- [ ] 2.1 新增 `refresh-token.entity.ts`（RefreshToken entity：userId/tokenHash/status/expiresAt/revokedAt）
- [ ] 2.2 `AuthModule` 注册 `TypeOrmModule.forFeature([RefreshToken])` + JWT 双 secret（access/refresh 独立密钥 + 独立 expiresIn）
- [ ] 2.3 `AuthService.login` 改为签发 access（token_type=access, 2h）+ refresh（token_type=refresh, 7d），refresh 落库 sha256 hash
- [ ] 2.4 新增 `AuthService.refresh`：校验 token_type=refresh + DB 账态（active 且未过期）→ 签发新 access
- [ ] 2.6 `AuthController` 新增 `POST /auth/refresh`、`POST /auth/logout`（`@Public()` + 独立校验）
- [ ] 2.7 `JwtAuthGuard` 增加 `token_type === 'access'` 校验，拒绝 refresh token 误当 access 使用

**验证要求**：| ID | 验证项 | 前置/数据准备 | 方法 | 预期结果 |
|-----|--------|---------------|------|---------|
| V-backend-1 | refresh token 落库 + 绝对过期 | 有可登录员工（sys_employee 有记录） | 单元测试调 `login` → 断言 `refresh_token` 表新增 active 记录且 expiresAt = 签发时间 + 7d；过期 token 刷新返回 401 | 落库成功，过期时间正确，过期后刷新 401 |
| V-backend-2 | refresh token 存 hash 非明文 | 同上 | 单元测试调 `login`，断言 `refresh_token.tokenHash` 为 sha256（64 hex），且不等于返回给前端的明文 refreshToken | 落库为 hash，明文不落库 |
| V-backend-3 | 员工停用联动吊销 | 有员工记录 | 单元测试 `revokeUserTokens(userId)` 后，该用户历史 refresh token 刷新返回 401 | 停用/吊销后刷新失败 |
| V-backend-4 | token_type 区分 | 已签发 access + refresh token | 用 refresh token 调带 `JwtAuthGuard` 的业务接口 → 401；用 access token 调 `refresh` → 401 | 类型隔离正确 |

---

## Sub-agent 返回契约

**完成后调用 `pg-build-result` 生成返回值并落盘，不要手写 JSON。**

### 必填字段（本阶段：dev）

| 字段 | 本阶段要求 |
|------|-----------|
| `--status` | 必填（见下方 status 矩阵） |
| `--summary` | 必填，<= 500 字 |
| `--track` | 必填（如 `dev.backend`） |
| `--phase` | 必填（当前阶段） |
| `--report` | 选填 |
| `--evidence` | 选填 |
| `--tasks-updated` | **必填**（实现的 task_id 列表，可多次传或逗号分隔） |
| `--outputs` / `--issues` | 选填 |

### status 矩阵（本阶段允许）

| phase=`dev` | `completed` / `failed` |

### 调用命令

```bash
python3 .pg/skills/src/core/workflows/skills/pg-build/scripts/pg-build-result \
    --mode agent \
    --status <status> \
    --summary "<=500 字>" \
    --track <dev.xxx> \
    --phase dev \
    --output-path D:\04-profile\test\house_pro4\house_pro2\.pg\changes\auth-token-refresh\2-build\002-dev.backend-dev-result.json \
    --require-output \
    [--report <绝对路径>] \
    [--evidence <绝对路径>]... \
    [--outputs <p1>,<p2>,...] \
    [--issues "<issue1>,<issue2>,"] \
    [--tasks-updated <id>]...
```

- `D:\04-profile\test\house_pro4\house_pro2\.pg\changes\auth-token-refresh\2-build\002-dev.backend-dev-result.json` 来自本 dispatch 上下文注入，**直接照抄**
- **A3 修复（v3.x）**：result.json 缺失 = orchestrator 直接 fatal `result_json_missing_after_retry`，**不再自动重试**（v2.4 重试机制会让一次 sub-agent 卡死掩盖整个 track 失败）。必须严格按命令模板执行 `--output-path D:\04-profile\test\house_pro4\house_pro2\.pg\changes\auth-token-refresh\2-build\002-dev.backend-dev-result.json --require-output`，不要漏写。
- 脚本 exit 1 → 字段校验失败，stderr 输出错误信息，照提示修复后重跑
- **gate / final-gate**：summary 末尾追加 `"gate_score: <0-100>, p0_failures: [...]"`，否则 schema_violation