PASS

# Verify Report: dev.backend — auth-token-refresh

> 阶段：verify（dev 集成验证）｜环境：dev-local（environment.required=true）
> 服务：backend-1 localhost:3000（PID 93540，本次验证前已用最新 `npm run build` 产物重启）
> 证据文件：`2-build/auth-token-refresh.verify-evidence.md`

## 结果总览

| 任务 | 结果 | 说明 |
|------|------|------|
| 4.1 lint | FAIL（既存豁免） | 项目缺少 ESLint 配置文件（`eslint couldn't find a configuration file`），002-dev 阶段已记录同一既存问题，与本次变更无关 |
| 4.2 单元测试 | PASS | `npm test`：Test Suites 2/2，Tests 15/15 passed，Failures 0，Errors 0 |
| 4.3 启动服务 | PASS | 最新 dist 重启后端口 3000 LISTENING，`/api/auth/refresh` 由 404→401 确认新代码生效 |
| 4.4 V-backend-1 | PASS | login 后 `refresh_token` 表新增 active 记录；expiresAt−createdAt=7.0d（绝对过期）；过期刷新 401 由单测覆盖 |
| 4.4 V-backend-2 | PASS | DB `tokenHash` = sha256(refreshToken)（64 hex，逐字符比对 match:true），明文不落库 |
| 4.4 V-backend-3 | PASS | logout 后 DB 行 `status=revoked` + `revokedAt` 落时间；再用该 refreshToken 刷新 → 401「Refresh token 无效或已注销」；revokeUserTokens 批量吊销由单测覆盖 |
| 4.4 V-backend-4 | PASS | refresh token 调业务接口 `/api/system/employees` → 401；access token 调 `/api/auth/refresh` → 401；access token 调业务接口 → 200 |

## 运行时启动 + 健康探针（B3 强制项）

1. **启动**：dispatch 的 `restart_all_instances` hook 在 Windows 下因 bash 子进程 PATH 缺 npm 失败（PG-E-0925，hook/环境兼容性问题，非应用代码问题）。改用直接方式重启：`nest build` 通过 → 停旧进程（PID 93452，`node dist\main`，14:01 启动的旧代码）→ 新进程 PID 93540 起最新 dist。
2. **就绪探针**：`netstat` 确认 3000 LISTENING（PID 93540）；HTTP 探针 `/` 返回 404（无根路由，正常，全局前缀 `/api`）；`/api/auth/refresh` 返回 401（端点存在）。
3. **真实 e2e**：login → 得双令牌（access 2h / refresh 7d，token_type 正确）→ DB 落库核验 → refresh 换新 access → 类型隔离三向核验 → logout 吊销 → 再刷新 401。全链路输出见 evidence 文件。
4. **失败处置**：无失败。

## NestJS DI 装配自查（Spring Bean 自查的 TS 对应项）

AuthService 单构造函数，构造器注入 `@InjectRepository(Employee/Role/RefreshToken)` + `JwtService` + `ConfigService`，全部 provider 已在 AuthModule 注册；无多构造函数、无"测试替身+生产 stub"模式。Nest 应用引导成功（运行中）即装配正确。

## Issues

无代码 bug。lint 失败为项目既存 ESLint 配置缺失（非本次变更引入，dev 阶段已记录），不构成 escalate。

## 结论

**PASS** —— V-backend-1~4 全部在真实运行服务 + 真实 PostgreSQL 上验证通过，单元测试 15/15 PASS。track `dev.backend` verify 阶段判定 `completed`。
