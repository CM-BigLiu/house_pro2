# house_pro2 · 第 16 轮验证报告（2026-08-28）

> 背景：本日早些时候完成 UI 整体改版提交（1a14621 系列）后，进行提交后回归验证
> 方式：浏览器逐页回归（23 页）+ 自动化符合性脚本 + 数据库核对
> 结果：发现并修复 **5 处真实缺陷**，全量回归 23/23 页面通过，符合性脚本 20/20

---

## 一、提交后回归发现的缺陷

| # | 缺陷 | 级别 | 根因 | 状态 |
|---|---|---|---|---|
| D1 | 角色权限配置面板显示「暂无权限数据」，角色权限**无法配置** | P1 | 种子写入权限时未设 parentId，28 个 action + 23 个二级菜单 parentId 全为 NULL，`/system/permissions/tree` 的 buildTree 全部落为顶层、菜单 children 恒为空；前端 `permGroups` 过滤「menu 且有 children」后为空 | ✅ 已修 |
| D2 | 门店管理页 `StoreView.vue` **页面空白**（模块加载失败） | P1 | 删死代码时连带删除了模板的闭合 `</div>`，Vue 编译报 `Element is missing end tag` | ✅ 已修 |
| D3 | 门店列表显示「共 0 家门店」（实际 9 家） | P1 | `/system/stores` 返回**数组**，前端按 `{list,total}` 解析 | ✅ 已修（兼容两种形态） |
| D4 | 门店**无法新增/编辑/删除** | P2 | StoreController 只有 GET，无 POST/PUT/DELETE | ✅ 已修（补写接口） |
| D5 | 新增的 5 个页面**无路由入口**、门店管理缺权限码 | P2 | `system:store` 等权限码未定义；StoreView/LogView/ConfigView/CheckoutView/DepositView 未接路由 | ✅ 已修 |

## 二、修复明细

### D1 权限树层级（提交 470b475）
- 种子 `seedPermissions` 补顶层菜单 module，写入后调用新增的 `linkPermissionHierarchy()`
  推导层级（二级菜单挂模块、action 按前缀最长匹配 + 语义兜底映射）
- `onModuleInit` 在已有数据分支也调用，历史库自动修复
- 新增一次性迁移脚本 `scripts/migrate-permission-tree.js`（幂等）
  并注册 `npm run migrate:permission-tree`
- 实测：权限树顶层 4 模块（home/house/finance/system），层级完整；
  角色面板渲染 26–41 个权限复选框，勾选保存 PUT 200 生效

### D2/D3 门店页（提交 bcbd8d3）
- 补齐 `</div>` 闭合标签
- `loadStores` 兼容数组与 `{list,total}` 两种返回
- 店长显示兼容关联对象与字符串（`managerName()` 取值）

### D4 门店写接口（提交 bcbd8d3）
- `StoreController` 新增 `POST /system/stores`、`PUT /system/stores/:id`、`DELETE /system/stores/:id`
- `StoreService` 新增 create/update/remove，findAll 关联 manager
- 「店长」文本输入按姓名匹配员工转 managerId（实测「李业务员」→ id=4）

### D5 页面接入与权限点（提交 bcbd8d3）
- 新增路由：`/system/store`、`/system/log`、`/system/config`、`/house/checkout`、`/house/deposit`
- 新增权限点：菜单 `system:store`、`system:log`、`system:config`、`house:checkout`、`house:deposit`；
  操作 `system:store:edit`、`checkout:export`、`deposit:export`
- 同步写入种子并授权 super_admin / company_admin

## 三、回归验证结果

### 3.1 全量页面（23 页，全部通过）

| 模块 | 页面 | 结果 |
|---|---|---|
| 首页 | 看板（KPI/预警/排行/趋势/待办） | ✅ |
| 房屋 | 出租房源、售房管理、储备房源、储备客源、客源管理、黑名单、小区、录入向导、退租管理、押金管理 | ✅ |
| 财务 | 账单、流水账、支出管理、开票管理、利润分析 | ✅ |
| 系统 | 角色、权限、字典、人员、门店、操作日志、系统配置 | ✅ |

无 JS 报错（`is not defined` / `Failed to fetch dynamically imported module` 均为 0）。

### 3.2 自动化符合性脚本（20/20）

`conformance_check_v2.mjs` 全部通过：401 语义、统一响应、脱敏、售房字段、
状态机（拦截+流转）、审批流、黑名单三维、看板、字典树形、PRD 别名、上传、
财务/系统接口、数据范围隔离（super=5 / agent02=1）。

### 3.3 核心链路抽测
- 门店：列表 9 家 → 新增（含店长匹配）→ 落库 → 删除，全链路通
- 角色：选中 → 权限树勾选 → 保存 PUT 200 → 权限数更新

## 四、遗留事项

| 级别 | 项 | 说明 |
|---|---|---|
| P2 | 退租管理、押金管理**后端接口未实现** | 前端页面已就绪（`/house/checkouts`、`/house/deposits`），后端无对应路由，列表恒为空 |
| P2 | 门店列表「员工数」「城市」显示 `--` | 种子数据未填充 employeeCount / cityName |
| P3 | 看板完整版（30 卡拖拽/6 视图/老板视图） | PRD H-025~044 |
| P3 | 日期 ISO 格式化、地址拼接后缀 | 体验优化 |

## 五、结论

提交后的回归验证暴露了 5 处真实缺陷（其中 D1 是**阻断性**的：角色权限无法配置），
全部已修复并回归通过。当前状态：
- 23 个页面全部可访问且无 JS 错误
- 自动化符合性检查 20/20
- 核心业务链路（录入 → 审批 → 状态流转 → 日志 → 财务收付 → 门店管理）真实可用

> 建议：退租/押金两个模块的后端接口作为下一迭代优先项补齐，使前端已就绪页面真正可用。
