# 功能测试阶段记录（2026-09-09）

> 后续进展：Chrome 连接已恢复并完成真实点击回归。以下为早期阶段记录；最新修复、测试结果及验收边界请见《Chrome点击测试与修复复测报告-20260909.md》。

## 本轮结论

功能回归仍在进行，不能据此判定项目整体验收通过。前端单测 12/12、后端单元及集成测试 32/32、后端 E2E 6/6 均通过。本轮 Chrome 已成功连接并登录，但点击交互未能可靠完成，随后浏览器工具超时；原生 Computer Use 又因无法可靠识别当前浏览器 URL 而停止。未完成的 Chrome 点击场景不计为通过，也不直接判为产品缺陷。

## 已完成的修复与验证

| 项目 | 实施及验证情况 |
| --- | --- |
| 租房编辑缺少接口 | 已实现 GET/PUT `/house/rental-sets/:id`，补详情读取、事务更新、房间归属与数据范围校验；前一轮内置浏览器中加载并保存成功。 |
| 黑名单越权 | 前端列表路由改用 `house:blacklist`，后端列表补相同权限；财务账号直达列表被重定向，真实接口返回 403；E2E 覆盖仅有客户权限时拒绝访问。 |
| 黑名单/小区权限码语义 | 新增独立 create/edit/delete 操作权限并接入前端按钮、路由和后端。内置角色已同步新权限。当前默认映射仍给店长黑名单新增/编辑能力；该角色是否应保有此能力尚未作为业务规则验收关闭。 |
| 租房编辑日期为空时报错 | 编辑更新将空日期归一化为 null；回归单测通过，前一轮浏览器保存成功。 |
| 黑名单编辑读到脱敏值 | 编辑详情在具备编辑权限和数据范围约束时返回原始字段，避免把掩码保存回数据库；列表仍脱敏，检查接口不返回手机号及身份证字段。 |
| 小区编辑入口 | 补详情及更新接口、编辑路由和表单；前一轮浏览器按原值保存成功。经纬度转换为数字后复查无组件类型警告。 |
| 新权限初始化 | 接入 SeedModule 并移除重复数据库根配置；独立 seed 入口避免重复执行初始化。 |

## 浏览器记录

此前内置浏览器已完成：租房编辑加载和按原值保存、小区编辑加载和按原值保存、黑名单编辑字段读取、财务账号黑名单路由拒绝访问、店长按钮权限展示。此前测试账号已退出登录。

本轮用户明确指定 Chrome 后，Chrome 扩展已连接，成功打开 `http://localhost:5173/login` 并登录超级管理员。首页和租房新增页均可加载；输入框可输入测试文本，但按钮点击、单选框切换和下拉选择未可靠触发。未提交本轮新增业务数据。

Chrome 交互工具随后超时。Computer Use 返回停止原因：无法可靠判断 Windows 当前浏览器 URL，因而不能继续执行 UI 操作。本轮 Chrome 登录会话未能经 UI 退出；继续测试时需先检查并退出或复用这个测试会话。本轮未触碰用户原有浏览器标签的业务内容。

## 已完成自动化检查

| 检查 | 结果 |
| --- | --- |
| 前端 Vitest | 2 文件，12/12 通过 |
| 后端 Jest | 7 套件，32/32 通过 |
| 后端 E2E | 2 套件，6/6 通过 |

上述测试于本轮重新执行并取得退出码 0。前后端 lint 已启动，但在本阶段记录生成时尚未取得最终结果，不列为通过。最新完整生产构建尚未在本阶段重新完成。

## 仍需完成

1. 恢复 Chrome 连接与可靠点击后，完成必填校验、整租/合租切换、查询筛选、新增后读取、编辑后保存及角色切换回归。
2. 单独确认店长是否应获得黑名单新增/编辑权限；当前独立权限码修正不等于业务授权策略已验收。
3. 收集 lint 最终结果，完成最新代码生产构建与差异格式检查。
4. 删除、退租、退款、扣款等具有业务后果的操作尚未在本轮真实页面提交，不计入点击验收通过范围。

本记录补充此前报告，不能替代完整项目验收结论。
